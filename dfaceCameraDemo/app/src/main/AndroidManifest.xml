<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pinhole.demo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity" android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" android:resource="@xml/device_filter" />

        </activity>


        <activity
            android:name=".activity.RegFaceActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.AddFaceActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.PhotoCompareActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.AuthActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.TrackerActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.SettingsActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".activity.UserListActivity"
            android:screenOrientation="unspecified" />

    </application>

</manifest>
